# Production Environment Configuration
server.port=8080

# Production Database Configuration
spring.datasource.url=jdbc:mysql://christmas-tracker-1.cq9qo0m4syyf.us-east-1.rds.amazonaws.com:3306/christmas_tracker
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=admin
spring.datasource.password=${DB_PASSWORD}

# JPA Configuration for Production
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Production CORS (allow production frontend)
spring.web.cors.allowed-origins=http://34.228.58.72

# Logging
logging.level.root=WARN
logging.level.com.example=INFO
